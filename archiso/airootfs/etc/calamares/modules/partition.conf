# Aegis Linux - Partition Module Configuration
# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0

# EFI System Partition configuration
efi:
    mountPoint: "/efi"
    recommendedSize: 512MiB
    minimumSize: 256MiB
    label: "EFI"

# User swap choices
userSwapChoices:
    - none
    - small
    - suspend
    - file

# Swap hibernate feature
swapHibernate: true

# Draw nested MBR partitions
drawNestedPartitions: false

# Always show partition labels
alwaysShowPartitionLabels: true

# Initial swap size for small
initialSwapChoice: none

# Allow manual partitioning
allowManualPartitioning: true

# Default filesystem
defaultFileSystemType: "ext4"

# Available filesystems
availableFileSystemTypes:
    - ext4
    - btrfs
    - xfs
    - f2fs

# Encryption settings
# LUKS2 with Argon2id
luksGeneration: luks2

# Enable LUKS encryption option
enableLuksAutomatedPartitioning: true

# Initial partition layout (for automatic partitioning)
initialPartitioningChoice: erase

# Allow choosing between replace, alongside, erase, manual
allowZfsEncryption: false
